---
name: translate-x-platform-article
description: "当用户发送“翻译X平台外放文章 + 知乎链接”时使用：准确读取目标文章或回答，并改写为适合海外 AI/tech 受众的简短英文 X 帖；正文无 emoji、hashtags、链接和 em dash，链接另放评论。"
---

# 翻译 X 平台外放文章

## 触发与目标

当用户提供“翻译 X 平台外放文章”及知乎文章、回答、想法或用户提供的原文时，输出适合 Zhihu Frontier 发布的英文 X post。

这不是逐字翻译。先理解原文，再保留核心事实、论证和作者判断，以 English-native tech audience 易于快速阅读的顺序改写。不得虚构原文没有的事实、因果、作者立场或主观判断。

用户针对单篇稿件提出的要求优先于本 skill 的默认规则。

## 原文读取

只读取目标内容的标题、作者昵称和正文。不要混入广告、评论、其他回答、侧栏、热榜或推荐内容。

必须尽力完整读取用户指定的原文，不得用搜索摘要、其他报道或相似文章替代。若目标正文确实无法读取，明确说明阻碍，并请用户提供原文或附件。

答主昵称保持原样，不翻译。统一写作 `Zhihu contributor *昵称*`，不要写 `Zhihu author`，不要在昵称前加 `@`。

## 译前判断

先提炼四项内容：

1. 原文最强结论或最反常识发现。
2. 支撑结论的最关键机制、数字或案例。
3. 作者真正想表达的意义或限制。
4. 哪些背景、benchmark、参数和例子可以删除而不损害理解。

如主题存在最近几天内高度相关、可信且能解释“为什么现在值得读”的热点，可用一句话自然承接。优先一手来源。没有合适热点就跳过，不硬蹭，不使用陈旧或弱相关事件。

外部热点与原文观点必须清楚区分。不要把外部信息写成原作者的观点。

## 写作原则

- 第一屏直接给最强结论、最意外数字或最清晰矛盾，不先写常规发布背景。
- 使用自然、克制、准确的英文，像技术从业者在 X 上解释一件事，不像新闻稿、论文摘要或中文直译。
- 优先短句和中短句。避免长难句，也避免连续多行同句式的排比短句。
- 每段通常 1 至 3 句，只表达一个主要信息。
- 只保留能强化观点或帮助理解的信息。不堆 benchmark、参数、背景或重复解释。
- 技术机制要说清楚，但只解释到理解核心结论所需的程度。
- 保留关键数字、方法名、能力差异、生产结果、限制和作者最重要的判断。
- 对比、步骤或并列事实确有必要时可使用普通 Markdown bullets 或数字列表，但不要使用表格。
- 不使用 emoji。
- 不使用 hashtags。
- 正文任何位置都不得使用 em dash 字符 `—`。改用句号、逗号、冒号、分号或括号。
- 不用夸张的 `BREAKING`、空泛拔高或没有原文依据的“行业最大事件”等判断。

## 长度

默认将正文控制在约 700 至 1200 个英文字符，不含标题和评论链接。

技术密度很高、过度压缩会损害理解时，可放宽到约 1500 个英文字符。用户明确要求详细版时再扩展。

完稿前继续删除：

- 不影响主线的发布背景。
- 重复解释和同义改写。
- 次要 benchmark、参数和例子。
- 可由一个上位概念概括的细节。
- 没有明确作用的过渡句。

第一屏约前 280 个英文字符内必须出现最强信息。若关键结论仍在 `Show more` 之后，继续调整。

## 标题与结构

主帖按以下顺序组织：

1. 英文标题，使用 `# **Title**`。标题从海外 AI/tech 读者角度重写，不机械直译。
2. 第一段直接给核心结论以及它为什么重要。
3. 用一句自然的作者引入，例如 `Zhihu contributor *昵称* explains...`。
4. 正文使用 2 至 4 个短信息块。每块只讲一个重点。
5. 必要时使用加粗的小标题，格式为 `**Short heading**`。小标题不加 emoji，不加 `#`。
6. 最后用一小段落到原作者的意义判断、限制或值得关注之处，不空泛总结。

不要固定套用同一句作者介绍或过渡语。不要默认写 `TL;DR:`。

段落主题变化时留一个空白段。为避免复制到富文本编辑器后空行消失，可在该空白行放一个全角空格 `　`。

## 加粗

加粗只用于：

- 文章总标题。
- 短小标题。
- 少量关键概念、数字或结论。

不要整段加粗，不要让每句都有加粗词。

## 链接与评论

所有 URL 都从主帖移出。主帖中不得出现原文链接、论文链接、项目链接、招聘链接或外部热点链接。

将链接放在单独的评论文案中。默认输出：

```text
Full reading:
[原文链接]
```

若原文确实包含必须保留的关键链接，可在同一评论中用简短标签逐行列出。评论保持简洁，不使用 emoji、hashtags 或 em dash。

## 输出格式

最终给出两个可分别一键复制的 Markdown 代码块：

1. `Post`：仅标题和正文，不含任何 URL、emoji、hashtags 或 em dash。
2. `Comment`：仅简短链接说明和相关 URL。

除这两个代码块外，不附加长篇解释。若存在无法核实的事实或读取限制，在代码块前用一句中文说明。

## 完稿检查

交付前逐项检查：

- 是否完整读取了用户指定的原文，而非用其他材料代替。
- 是否忠实保留原文核心观点，没有添加主观判断。
- 前 280 个字符是否已经出现最强信息。
- 是否足够短，且每段只有一个重点。
- 作者是否写为 `Zhihu contributor *原昵称*`。
- 主帖是否完全没有 URL、emoji、hashtags 和 `—`。
- 所有链接是否只出现在单独评论中。
- 是否没有 Markdown 表格。
- 是否使用自然、准确、English-native 的技术英文。
